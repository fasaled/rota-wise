/**
 * All Excel functionality: export .xlsx, open/drop .xlsx, metadata-mode
 * editing, and writing Excel files. Implementation is kept in place.
 *
 * Set to `true` to re-enable.
 */
export const ENABLE_EXCEL = false;
